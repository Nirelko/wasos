import {createMuiTheme} from '@material-ui/core';
import {lightBlue, amber, red} from '@material-ui/core/colors';

export default createMuiTheme({
  palette: {
    primary: lightBlue,
    secondary: amber,
    error: red
  }
});