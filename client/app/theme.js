import { createMuiTheme } from 'material-ui/styles';
import { lime, amber, red } from 'material-ui/colors';

export default createMuiTheme({
  palette: {
    primary: lime,
    secondary: amber,
    error: red
  }
});